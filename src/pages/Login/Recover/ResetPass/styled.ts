import styled from "styled-components";

export const Resend = styled.span`
  color: var(--bg-dark);
  text-align: center;
  font-family: sans-serif;
  font-size: .8rem;
  cursor: pointer;
  margin-top: 10%;
  width: 80%;
`;
